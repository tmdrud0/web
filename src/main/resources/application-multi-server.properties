contest.scoreboard.store=redis
contest.submission.dedup.store=redis
contest.submission.rate-limit.store=redis
contest.submission.id.strategy=snowflake
spring.session.store-type=redis
spring.datasource.hikari.maximum-pool-size=100
spring.datasource.hikari.minimum-idle=20
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# Actuator listens on its own port on every role. nginx only proxies 8080, so
# /actuator is reachable from Prometheus inside the compose network and nowhere else.
management.server.port=9000
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
